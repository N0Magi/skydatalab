export default {
    nWidth: 204, // 节点宽度
    nHeight: 32, // 节点高度
    nDy: 6, // 节点文件默认y轴偏移距离
    strokeWidth: 1, // 默认画笔宽度
    cellWidth: 20, // 网格宽度
    cellHeight: 20, // 网格高度
    autoAlignment: 4, // 辅助线自动对齐范围
    precision: 1, // 辅助线对齐精度
    scaleRange: [0.5, 2], // 缩放范围
    leftSpace: 10, // 详情框 距左侧 距离
    topSpace: 15, // 详情框 距上侧 距离
    fontLineSpace: 16, // 详情框 行间距
    detailFontSize: 12, // 详情框 字体大小
    iconTopSpace: 9, // 图标距上侧 距离
    detailBoxHeight: 160, // 详情框 高度
    pointRadius: 8, // 连接点半径
    pointRadiusMax: 10 // 连接点放大半径
    // detailBoxHeight: 210
};
